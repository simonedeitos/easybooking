# Block direct web access to cron scripts
# They should only be executed via CLI (or with a secret token)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
