# ── Database ─────────────────────────────────────────────────────
# Copy this file to .env (in the webapp/ directory) and fill in your values.
# The .env file is loaded automatically by config/database.php.
# NEVER commit your .env file to version control.

DB_HOST=mysql
DB_HOST_FALLBACK=localhost
DB_NAME=u362062795_easybooking
DB_USER=u362062795_easybooking
DB_PASS=D4tabas3-EasyB00k1ng-vocefutura
